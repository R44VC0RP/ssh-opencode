# SSH OpenCode Configuration
# Copy this to .env and fill in your values

# =============================================================================
# CLOUDFLARE DEPLOYMENT (Required for production/CF testing)
# =============================================================================

# Your Cloudflare Account ID
# Find at: https://dash.cloudflare.com > Workers & Pages > Overview (right sidebar)
CLOUDFLARE_ACCOUNT_ID=

# Cloudflare API Token
# Create at: https://dash.cloudflare.com/profile/api-tokens
#
# Option 1: Use "Edit Cloudflare Workers" template, then ADD these permissions:
#   - Account > Workers R2 Storage > Edit
#   - Account > Workers Scripts > Edit (should already be included)
#   - Account > D1 > Edit (for Durable Objects)
#
# Option 2: Create Custom Token with these permissions:
#   Account Permissions:
#     - Workers R2 Storage: Edit
#     - Workers Scripts: Edit  
#     - D1: Edit
#   Zone Permissions (if using custom domain):
#     - Workers Routes: Edit
#
# Make sure to select your account under "Account Resources"
CLOUDFLARE_API_TOKEN=

# =============================================================================
# AFTER DEPLOYING WORKER - Set this for VPS/local ssh-relay
# =============================================================================

# WebSocket URL to your deployed Cloudflare Worker
# Format: wss://opencode-relay.<your-subdomain>.workers.dev/ws
# You'll get this URL after running: cd packages/worker && npm run deploy
WORKER_URL=

# =============================================================================
# OPTIONAL - Security
# =============================================================================

# Shared secret between VPS and Worker (recommended for production)
# Generate with: openssl rand -hex 32
# AUTH_SECRET=

# =============================================================================
# OPTIONAL - Customization
# =============================================================================

# Idle timeout before container sleeps (minutes, default: 30)
# IDLE_TIMEOUT_MINUTES=30

# GitHub token for cloning private repos
# GITHUB_TOKEN=ghp_xxxxxxxxxxxx
